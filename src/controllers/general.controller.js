// const httpStatus = require('http-status');
// const pick = require('../utils/pick');
// const getPeerFilter = require('../utils/getPeerFilter');
// const ApiError = require('../utils/ApiError');
// const catchAsync = require('../utils/catchAsync');
// const { orderService, flightService, customerService, userService } = require('../services');

// const counterDashboard = catchAsync(async (req, res) => {
//   const code = generateCode();
//   const status = 'PACKAGED';
//   const order = await orderService.count({ ...payload, code, status });
//   res.status(httpStatus[200]).send(order);
// });
